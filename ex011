#11 faça um programa que leia a largura e a altura da parede em metros e calcule a sua área e a
# de tinta necessária para pinta-la, sabendo que cada litro de tinta pinta uma area de 2m²

#altura = float(input('Digite a altura da sua parede separado por ponto: '))
#larg = float(input('Digite a largura da sua parede separado por ponto: '))
#area = altura * larg
#print('Voce possue: {}² e desta forma precisa de: {} litros de tinta'.format(area, area / 2))
#certo com menos variaveis

altura = float(input('Altura da parede: '))
larg =  float(input('Largura da parede: '))
area = larg * altura
print('Sua parede tem a dimenção de {}x{} e a sua area é de {}m²'.format(altura, larg, area))
tinta = area / 2
print('Para pintar a parede voce precisa de {}l de tinta'.format(tinta))
